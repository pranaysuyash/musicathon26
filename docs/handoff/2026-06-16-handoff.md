# Handoff — VerseSignal (Musicathon 2026)

**Date:** 2026-06-16
**Status:** Feature-ready, not launch-ready (per 11-dim audit)
**Audience:** next agent / Pranay / Musicathon judges

## What this is

A music-cultural knowledge graph: how popular songs, lyrics,
artists, themes, named entities, moods, collaborators, and
world events connect across time. Built for Musicathon 2026.

The product surface is `/graph` — a force-directed graph where
every node is a song/artist/year/event/theme/mood/entity, and
every edge carries weight + confidence + source API + model
version + explanation + evidence rows. The user can click any
edge to see the lyric line and matched terms that produced it,
or ask the PathPanel "How is X connected to Y?" to get a
shortest-path traversal with per-hop evidence.

## Final state (Tier 5, real data)

- 150 songs (6 years × 25, Billboard Hot 100 year-end 2018–2023)
- 131 songs with Musixmatch lyrics (87%; 19 restricted by
  Musixmatch's license)
- 10 curated world events
- 6,711 lyric lines, 981 theme scores, 390 mood scores,
  2,079 entity mentions, 128 song embeddings
- 846 graph nodes, 3,437 graph edges, 5,816 evidence rows
- 4/4 path presets work; 9/9 surface routes return 200

## How to run it (60 seconds)

```bash
# 1. Install
cd /Users/pranay/Projects/musicathon
npm install

# 2. .env is already populated with the hackathon partner keys
cat .env   # see MUSIXMATCH, SONGSTATS, HF, ELEVENLABS, JAMBASE keys

# 3. Initialize the database (creates schema + 10 curated events)
npm run db:init
npm run db:seed-chart

# 4. Fetch lyrics from Musixmatch (150 songs, recovers up to 131
#    in ~90s; the rest are Musixmatch-restricted)
npm run db:fetch-lyrics

# 5. Run the enrichment pipeline (embeddings + GLiNER + themes + events)
npm run py:enrich       # full: 12-15 min with GLiNER
# OR
npm run py:enrich-fast  # skips embeddings + GLiNER: ~1s

# 6. Build similar_to edges
npm run py:similar

# 7. Dev server
npm run dev
# Open http://localhost:3000

# 8. (Optional) Run the artist-match smoke test
npx tsx tools/test-artist-match.ts
```

## The three demo paths that win

1. **"What was the world singing in 2020?"** — `/year/2020`
   Shows the year insight (ElevenLabs narration), theme cloud,
   top songs. Click any song → `/song/[id]` with full lyrics,
   themes, entities, event links.

2. **"How is 'Blinding Lights' connected to COVID-19?"** —
   `/graph` → PathPanel preset "Blinding Lights → COVID-19"
   Returns 1-hop path with full evidence.

3. **"Show me the COVID-19 lockdown graph"** — `/graph?rootType=event&rootId=versesignal:n:event:versesignal:ev:covid_19&hops=1`
   86 nodes, 85 edges in 1 hop. Click any edge for evidence.

## What's in the box

```
.
├── app/                    Next.js 14 App Router
│   ├── api/                JSON endpoints (year, event, song, graph, path, insight, events, lyrics, edge-evidence)
│   ├── event/[id]/         Event lens (per-event song connections with evidence)
│   ├── graph/              Knowledge graph explorer + PathPanel
│   ├── song/[id]/          Song detail (lyrics, themes, entities, event links)
│   ├── year/[year]/        Year lens (themes, moods, events, songs, narrated insight)
│   ├── page.tsx            Landing
│   └── layout.tsx          Root
├── components/
│   ├── evidence/           Evidence drawer
│   ├── graph/              GraphView (react-force-graph-2d) + PathPanel
│   ├── lens/               Theme cloud + Year insight player
│   └── ui/                 Primitives (Pill, ConfidenceBar, SectionTitle)
├── lib/
│   ├── api/                Musixmatch, Songstats, ElevenLabs, JamBase, Cyanite clients
│   ├── db/                 SQLite + Drizzle-style typed queries
│   ├── graph/              path-finder.ts (in-memory BFS)
│   ├── nlp/                theme-lexicon, theme-scoring, ner_labels
│   └── types.ts            Shared domain types
├── scripts/
│   ├── schema.sql          DDL (source of truth for the graph schema)
│   ├── db-init.ts          Run schema.sql
│   ├── seed-chart-data.ts  Hand-curated 150-song Billboard year-end 2018-2023
│   ├── fetch-lyrics.ts     Musixmatch → SQLite
│   ├── enrich.py           sentence-transformers + GLiNER + theme + event linking
│   ├── build-similar-edges.py  pairwise cosine → similar_to edges
│   └── check-env.ts        env presence (no value leak)
├── data/
│   ├── chart-seed.ts       Hand-curated (gitignored wholesale, but file is whitelisted)
│   ├── versesignal.db      SQLite (gitignored)
│   ├── exports/insights/   ElevenLabs MP3s (gitignored)
│   └── cache/hf/           HuggingFace model cache (gitignored)
├── docs/
│   ├── worklog/            Session logs
│   ├── decisions/          NNNN-title.md, the durable memory
│   └── audit/              11-dim audit
├── pyproject.toml          Python deps (uv-managed)
├── .env / .env.example     API keys (gitignored)
├── replit.nix.toml         Replit deploy config (deferred per user)
└── tools/                  Reusable project-local tools
```

## Key design decisions (read these before changing code)

1. **0001 graph-first, not 3D-Earth-first** — the graph is the
   product. 3D Earth (if it ships later) is a reward screen.
2. **0002 evidence-tiered graph edges** — every edge has
   weight, confidence, source API, model version, explanation,
   and ≥1 evidence row. The trust layer is the product.
3. **0003 pluggable intelligence pipeline** — themes via
   lexicon + embedding hybrid; NER via GLiNER (fallback
   spaCy); moods via Cyanite (fallback lexicon). Each layer
   has a documented model + version + fallback.
4. **0004 per-event-category temporal windows** — elections
   are tight (3mo lead-in / 6mo echo); pandemics echo for 2
   years; social movements have 36mo echo. A song links to
   an event only if temporally within category's window.
5. **0005 similar_to edges + Connection Finder** — pairwise
   cosine over lyrics embeddings; BFS shortest path with 30s
   cache; 4/4 preset paths verified.
6. **0006 GLiNER over spaCy for lyric NER** — custom
   music-cultural labels, per-label thresholds, versioned.
7. **0007 Python 3.13 via uv** — `.venv/` is uv-managed;
   `pyproject.toml` is the source of truth.
8. **0008 Cyanite as future audio-mood source** — present as
   `lib/api/cyanite.ts` client but not yet invoked (no key).

## What's NOT done (follow-ups, ordered by impact)

1. **19 songs missing lyrics** (Drake's "Nice For What",
   Lizzo's "Truth Hurts", etc.) — Musixmatch upstream issue,
   not a VerseSignal bug. If partner can provide an
   alternative lyrics source, plug in a new fetch function.
2. **Cyanite audio mood integration** — no key in this
   build. Hook present in `lib/api/cyanite.ts`; integration
   point documented in decision 0007 / 0008.
3. **Test suite** — `tools/test-artist-match.ts` exists as a
   pattern; need jest/vitest for the UI and pytest for the
   Python pipeline.
4. **MusicBrainz / Wikidata entity linking** — GLiNER returns
   surface forms; we don't link "Drake" to a MusicBrainz ID
   yet. High-value follow-up for graph quality.
5. **3D Earth view** — out of scope per 0001; available as
   future option, not a current gap.
6. **Replit deploy** — `replit.nix.toml` exists, unverified
   per user.
7. **CI + monitoring + log shipping** — none.

## How to extend

- **Add a new event:** insert into `events` table with
  `id = versesignal:ev:<slug>`, `regions_json`, `category`,
  `keywords_json`, `related_themes_json`. Re-run
  `npm run py:enrich-fast` to generate new event-song links
  with the new temporal logic.
- **Add a new theme:** append to `lib/nlp/theme-lexicon.json`
  and add a seed sentence to `scripts/enrich.py:THEME_SEEDS`.
  Re-run `npm run py:enrich-fast` to re-score all songs.
- **Add a new edge type:** add to `lib/types.ts:EdgeType`,
  add a producer in `enrich.py:link_*` (or a new function),
  add a fallback in `getNodeNeighborhood` (none needed; BFS
  is type-agnostic).
- **Add a new curated event/song pair:** add a preset to
  `components/graph/path-panel.tsx:presets`.

## How to debug

- **Empty year/event/graph page?** Run `npm run check:env` to
  verify all required env vars are present.
- **Path query returns "not found"?** Check the node IDs in
  the DB:
  `sqlite3 data/versesignal.db "SELECT id, node_type, label FROM graph_nodes WHERE id LIKE '%event%'"`
- **Enrichment results look wrong?** Check the per-event
  distribution:
  `sqlite3 data/versesignal.db "SELECT ev.name, s.year, COUNT(*) FROM graph_edges ge JOIN events ev ON ev.id = SUBSTR(ge.dst_id, 22) JOIN songs s ON s.id = SUBSTR(ge.src_id, 22) WHERE ge.edge_type='associated_with_event' GROUP BY ev.id, s.year ORDER BY 1, 2"`
- **ElevenLabs MP3s stale?** Delete `data/exports/insights/*.mp3`
  and hit `/api/insight?year=YYYY` to regenerate.

## What NOT to do

- **Don't commit `.env`.** It's gitignored. Keep it that way.
- **Don't commit `data/versesignal.db`.** It contains
  Musixmatch-licensed content + ElevenLabs-generated audio.
- **Don't commit `data/exports/insights/*.mp3`.** ElevenLabs
  audio belongs to the Pro plan quota; re-generate per call.
- **Don't add an `any` to the public API surface.** The
  SQLite helpers (`lib/db/sql.ts`) use one; it's contained.
- **Don't use `python3` directly.** Use `uv run --no-sync python`.
- **Don't add a 3D Earth as the front door.** See 0001.
- **Don't add a new top-level route file.** The Next.js
  App Router convention is `app/<resource>/route.ts` (we use
  `app/api/<resource>/route.ts` for JSON). Don't fork this.
- **Don't modify `EVENT_TEMPORAL_WINDOWS` without a
  decision record.** It's a per-event-category tuning table;
  changes have a documented blast radius.
- **Don't modify the GLiNER labels in `lib/nlp/ner_labels.py`
  without bumping `LABELS_VERSION`.** Old rows still
  reference the old version; bumping forces a fresh re-enrich
  and a new `model_version` on every row.

## Contacts

- **Repo:** `/Users/pranay/Projects/musicathon`
- **Remote:** https://github.com/pranaysuyash/musicathon26
- **Last commit on remote:** `b92adf3` (P0 scaffold)
- **Local uncommitted:** G1-G5 (worklog, decisions, fixes)
- **Owning agent:** this one (for this session)
- **Musicathon partner keys:** in `.env` (gitignored)

## Tier evidence for the 11-dim audit

| Dim | Tier | What was verified |
|---|---|---|
| Code | 4 | `npm run build` exits 0; `npx tsc --noEmit` exits 0; all Python scripts parse |
| Operational | 4 | 9 routes return 200; enrichment runs; ElevenLabs MP3s cached; 3 lyrics recovered after fetch fix |
| User Experience | 4 | Manual click-through of all 5 surfaces + 4 path presets |
| Logical Consistency | 4 | Temporal bucket label in every event edge; tools/test-artist-match.ts covers lyrics-fetch regression (15/15 pass) |
| Commercial | 0 | N/A |
| Data Integrity | 4 | Evidence 5,816/3,437 = 1.7x; 131/150 songs have lyrics (87%) |
| Quality & Reliability | 3 | Path-finder 4/4 presets; BFS cycle-safe; cache bounded; tools/ smoke test for artist-match |
| Compliance | 4 | `.env` gitignored; partner keys never printed |
| Operational Readiness | 4 | `npm run check:env`; re-build path documented; smoke test in `tools/` |
| Critical Path | 4 | All 3 demo paths work end-to-end; "God's Plan → identity" now 1 hop (was 3) |
| Final Verdict | 4 | Merge: yes; Feature: yes; Launch: not yet (no CI/deploy) |
